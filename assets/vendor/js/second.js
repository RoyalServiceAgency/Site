$(document).ready(function() {
         $('#myCarouselPartners').carousel({
           interval: 90, // Changer le temps en millisecondes ici (3000ms = 3 secondes)
           wrap: true // Permet le défilement en boucle
         });
    
       });